//
//  LumenCore.swift
//  LumenCore
//
//  Non-UI heart of Lumen: vault management, file IO, FSEvents watching,
//  frontmatter parsing, and the SQLite note index.
//
//  P1.1 scaffold only — no functionality yet. Real implementations land in
//  later Phase-1 tasks (vault/bookmarks, index, FSEvents, frontmatter).
//

import Foundation

/// Namespace + version marker for the LumenCore module.
public enum LumenCore {
    /// Semantic version of the core module scaffold.
    public static let version = "0.0.1"
}
