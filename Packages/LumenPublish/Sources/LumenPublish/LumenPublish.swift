//
//  LumenPublish.swift
//  LumenPublish
//
//  P1.1 scaffold only — empty, compilable stub so later-phase wiring has a home.
//

import Foundation

/// Namespace + version marker for the LumenPublish module (deferred phase).
public enum LumenPublish {
    /// Semantic version of the LumenPublish module scaffold.
    public static let version = "0.0.1"
}
