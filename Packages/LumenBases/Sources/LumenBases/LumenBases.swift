//
//  LumenBases.swift
//  LumenBases
//
//  P1.1 scaffold only — empty, compilable stub so later-phase wiring has a home.
//

import Foundation

/// Namespace + version marker for the LumenBases module (deferred phase).
public enum LumenBases {
    /// Semantic version of the LumenBases module scaffold.
    public static let version = "0.0.1"
}
