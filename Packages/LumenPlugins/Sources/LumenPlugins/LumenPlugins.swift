//
//  LumenPlugins.swift
//  LumenPlugins
//
//  P1.1 scaffold only — empty, compilable stub so later-phase wiring has a home.
//

import Foundation

/// Namespace + version marker for the LumenPlugins module (deferred phase).
public enum LumenPlugins {
    /// Semantic version of the LumenPlugins module scaffold.
    public static let version = "0.0.1"
}
