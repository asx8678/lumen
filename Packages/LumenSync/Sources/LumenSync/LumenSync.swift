//
//  LumenSync.swift
//  LumenSync
//
//  P1.1 scaffold only — empty, compilable stub so later-phase wiring has a home.
//

import Foundation

/// Namespace + version marker for the LumenSync module (deferred phase).
public enum LumenSync {
    /// Semantic version of the LumenSync module scaffold.
    public static let version = "0.0.1"
}
